/**
 * JSON WEB TOKEN(jwt)*
 * Methode
 *
 * jwt.sign({UserInformation},privateKey,{delayOf expiration});
 *
 * jwt.verify(token,privateKey,()=>{callbackFunction})
 */
/**
 * Bcrypt authentication
 * bcrypt.hash(password,timeToHash)
 * return the password hashing
 *
 * bcrypt.compare(passwordGiven,passwordEncrypted)
 * return the Boolean
 * True:if password given is matching by the password encrypted in database otherwise return False
 */
